plugins {
    id 'java-platform'
}

group = 'com.ictodo.platform'

dependencies {
    constraints {
        api "io.quarkus:gradle-application-plugin:${quarkusVersion}"
        api "com.vaadin:vaadin-gradle-plugin:${vaadinVersion}"
    }
}
