pluginManagement {
    repositories {
        gradlePluginPortal()
    }

    includeBuild '../build-logic'
}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

includeBuild '../platform'

rootProject.name = 'web-ui'

include 'app'
