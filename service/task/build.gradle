plugins {
    id 'com.ictodo.quarkus-server'
}

group = "${group}.service"
version '1.0.0-SNAPSHOT'

dependencies {
    implementation 'com.ictodo.util:exception'
    implementation 'com.ictodo.model:data'
}
